'use client'
import {useEffect,useState} from 'react'
import {Button} from '@/components/ui/button'
import {Input} from '@/components/ui/input'
import {Badge} from '@/components/ui/badge'
import {Card,CardContent} from '@/components/ui/card'
import {Plus,Search,Users,Store,KeyRound,Power,RefreshCw} from 'lucide-react'

type P={id:string;kodeMitra:string;namaCabang:string;namaPic:string;whatsapp:string;alamat:string;isActive:boolean;orderCount:number;totalBelanja:number}
const rp=(n:number)=>new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(n)
export function AdminPartners(){const[ps,setPs]=useState<P[]>([]),[q,setQ]=useState(''),[form,setForm]=useState<any>(null),[busy,setBusy]=useState(false),[err,setErr]=useState('')
 const load=async()=>{const r=await fetch('/api/admin/partners',{cache:'no-store'});if(r.ok)setPs(await r.json())};useEffect(()=>{load()},[])
 const filtered=ps.filter(p=>`${p.kodeMitra} ${p.namaCabang} ${p.namaPic} ${p.whatsapp}`.toLowerCase().includes(q.toLowerCase()))
 const save=async(e:any)=>{e.preventDefault();setBusy(true);setErr('');const edit=!!form.id;const r=await fetch(edit?`/api/admin/partners/${form.id}`:'/api/admin/partners',{method:edit?'PUT':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const j=await r.json();if(!r.ok)setErr(j.error||'Gagal menyimpan');else{setForm(null);await load()}setBusy(false)}
 const toggle=async(p:P)=>{await fetch(`/api/admin/partners/${p.id}`,{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({isActive:!p.isActive})});load()}
 const reset=async(p:P)=>{const pin=prompt(`PIN baru untuk ${p.kodeMitra} (4-8 angka):`);if(!pin)return;const r=await fetch(`/api/admin/partners/${p.id}`,{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({pin})});if(!r.ok){const j=await r.json();alert(j.error)}else alert('PIN berhasil direset')}
 return <div className="space-y-5"><div className="grid grid-cols-2 md:grid-cols-3 gap-3"><Card><CardContent className="p-4"><p className="text-xs text-slate-500">Total Mitra</p><p className="text-2xl font-black mt-1">{ps.length}</p></CardContent></Card><Card><CardContent className="p-4"><p className="text-xs text-slate-500">Mitra Aktif</p><p className="text-2xl font-black mt-1 text-emerald-600">{ps.filter(p=>p.isActive).length}</p></CardContent></Card><Card className="col-span-2 md:col-span-1"><CardContent className="p-4"><p className="text-xs text-slate-500">Total Order Mitra</p><p className="text-2xl font-black mt-1">{ps.reduce((s,p)=>s+p.orderCount,0)}</p></CardContent></Card></div><div className="flex flex-col sm:flex-row gap-3 justify-between"><div className="relative flex-1 max-w-xl"><Search className="absolute left-3 top-3 h-4 w-4 text-slate-400"/><Input value={q} onChange={e=>setQ(e.target.value)} placeholder="Cari kode, cabang, PIC atau WhatsApp..." className="pl-9"/></div><div className="flex gap-2"><Button variant="outline" onClick={load}><RefreshCw className="h-4 w-4 mr-2"/>Refresh</Button><Button onClick={()=>setForm({kodeMitra:'',namaCabang:'',namaPic:'',whatsapp:'',alamat:'',pin:''})} className="bg-red-600 hover:bg-red-700"><Plus className="h-4 w-4 mr-2"/>Tambah Mitra</Button></div></div><div className="grid md:grid-cols-2 xl:grid-cols-3 gap-4">{filtered.map(p=><Card key={p.id} className="rounded-2xl"><CardContent className="p-5"><div className="flex justify-between"><div className="h-11 w-11 bg-red-50 rounded-xl flex items-center justify-center"><Store className="text-red-600"/></div><Badge className={p.isActive?'bg-emerald-50 text-emerald-700 hover:bg-emerald-50':'bg-slate-100 text-slate-500 hover:bg-slate-100'}>{p.isActive?'Aktif':'Nonaktif'}</Badge></div><h3 className="font-black text-lg mt-4">{p.namaCabang}</h3><p className="text-xs font-bold text-red-600">{p.kodeMitra}</p><div className="mt-4 text-sm space-y-1 text-slate-600"><p>PIC: <b>{p.namaPic}</b></p><p>WA: {p.whatsapp}</p><p className="truncate">{p.alamat||'Alamat belum diisi'}</p></div><div className="grid grid-cols-2 gap-2 mt-4 bg-slate-50 rounded-xl p-3"><div><p className="text-[10px] text-slate-400">PESANAN</p><b>{p.orderCount}</b></div><div><p className="text-[10px] text-slate-400">TOTAL BELANJA</p><b className="text-xs">{rp(p.totalBelanja)}</b></div></div><div className="grid grid-cols-3 gap-2 mt-4"><Button size="sm" variant="outline" onClick={()=>setForm({...p,pin:''})}>Edit</Button><Button size="sm" variant="outline" onClick={()=>reset(p)}><KeyRound className="h-3.5 w-3.5"/></Button><Button size="sm" variant="outline" onClick={()=>toggle(p)}><Power className={`h-3.5 w-3.5 ${p.isActive?'text-red-500':'text-emerald-600'}`}/></Button></div></CardContent></Card>)}</div>{form&&<div className="fixed inset-0 z-50 bg-black/40 flex items-center justify-center p-4"><form onSubmit={save} className="bg-white rounded-3xl p-6 w-full max-w-lg shadow-2xl"><h2 className="font-black text-xl">{form.id?'Edit Mitra':'Tambah Mitra'}</h2><div className="grid sm:grid-cols-2 gap-3 mt-5"><Input placeholder="Kode Mitra" disabled={!!form.id} value={form.kodeMitra} onChange={e=>setForm({...form,kodeMitra:e.target.value.toUpperCase()})}/><Input placeholder="Nama Cabang" value={form.namaCabang} onChange={e=>setForm({...form,namaCabang:e.target.value})}/><Input placeholder="Nama PIC" value={form.namaPic} onChange={e=>setForm({...form,namaPic:e.target.value})}/><Input placeholder="WhatsApp" value={form.whatsapp} onChange={e=>setForm({...form,whatsapp:e.target.value})}/><Input className="sm:col-span-2" placeholder="Alamat" value={form.alamat} onChange={e=>setForm({...form,alamat:e.target.value})}/>{!form.id&&<Input className="sm:col-span-2" type="password" inputMode="numeric" placeholder="PIN 4-8 angka" value={form.pin} onChange={e=>setForm({...form,pin:e.target.value.replace(/\D/g,'').slice(0,8)})}/>}</div>{err&&<p className="text-sm text-red-600 mt-3">{err}</p>}<div className="flex justify-end gap-2 mt-6"><Button type="button" variant="outline" onClick={()=>setForm(null)}>Batal</Button><Button disabled={busy} className="bg-red-600 hover:bg-red-700">{busy?'Menyimpan...':'Simpan'}</Button></div></form></div>}</div>}
